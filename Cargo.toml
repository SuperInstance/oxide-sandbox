[package]
name = "oxide-sandbox"
version = "0.1.0"
edition = "2021"
description = "Experimental sandbox testing the full Flux→MIR→PTX compile path end-to-end. Validates that agent-generated bytecode can produce valid GPU kernel descriptions."
license = "Apache-2.0"
repository = "https://github.com/SuperInstance/oxide-sandbox"
keywords = ["flux", "ptx", "gpu", "experiment", "compiler"]
categories = ["compilers"]
